spring:
  application:
    name: liteblog
  profiles:
    active: dev

server:
  port: 8080
  servlet:
    context-path: /api

# JWT 配置
jwt:
  secret: ${JWT_SECRET:liteblog-secret-key-for-development-only-change-in-production}
  expiration: 86400000  # 24小时（毫秒）
  issuer: liteblog
